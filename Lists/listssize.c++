#include <bits/stdc++.h>
using namespace std;

void lists() {
    list<int> l = {1 , 2, 3};

    // for (int val : l) {
    //     cout << val << " "; 
    // }
    cout << "The size of list is : " << l.size() << endl;
}

int main() {
  lists();
  return 0;
}
