#include <bits/stdc++.h>
using namespace std;

int main()
{
    int a , b;
    a = 20;
    b = 23;

    swap(a , b);

    cout << "a = " << a << endl;
    cout << "b = " << b << endl;

  
    cout << endl;
    return 0;
}