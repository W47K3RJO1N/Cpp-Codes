#include <bits/stdc++.h>
using namespace std;

void capacity () {
    vector<int> vec;
    vec.push_back(3);
    vec.push_back(4);
    vec.push_back(5);

    cout << vec.capacity() << endl;
    cout << vec.size() << endl;
}

int main() {
    capacity();
    return 0;
}
