#include <bits/stdc++.h>
using namespace std;

int main() {
    int a , b, c;

    cin >> a;
    cin >> b;
    cin >> c;
   
    cout << "The addition of a and b is: " << a + b;
    cout << "The addition of a and c is: " << b + c;
}
